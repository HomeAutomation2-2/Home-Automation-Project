# API (pe Windows cu Docker/Hyper-V, 3000 e adesea rezervat — foloseste 3500)
PORT=3500

# PostgreSQL (Docker din COD/DATABASES — port 5433)
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=mysecretpassword
DB_DATABASE=home_automation
